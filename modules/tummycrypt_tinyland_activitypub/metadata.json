{
  "homepage": "https://github.com/tinyland-inc/tinyland-activitypub",
  "maintainers": [
    {
      "name": "tinyland",
      "email": "tinyland@tinyland.dev"
    }
  ],
  "versions": [
    "0.2.2",
    "0.2.3",
    "0.2.4",
    "0.2.5"
  ],
  "yanked_versions": {
    "0.2.2": "TIN-2416: standalone module defaults ActivityPub actor/object origin to the apex host (transscendsurvival.org-class leak), superseded by the hub-anchored emitter (tinyland.dev PR #623). Apply only after tinyland.dev drops the bazel_dep (patches 02/03) and confirms zero remaining consumers via `bazel mod deps`.",
    "0.2.3": "TIN-2416: standalone module defaults ActivityPub actor/object origin to the apex host (transscendsurvival.org-class leak), superseded by the hub-anchored emitter (tinyland.dev PR #623). Apply only after tinyland.dev drops the bazel_dep (patches 02/03) and confirms zero remaining consumers via `bazel mod deps`.",
    "0.2.4": "TIN-2416: standalone module defaults ActivityPub actor/object origin to the apex host (transscendsurvival.org-class leak), superseded by the hub-anchored emitter (tinyland.dev PR #623). Apply only after tinyland.dev drops the bazel_dep (patches 02/03) and confirms zero remaining consumers via `bazel mod deps`.",
    "0.2.5": "TIN-2416: standalone module defaults ActivityPub actor/object origin to the apex host (transscendsurvival.org-class leak), superseded by the hub-anchored emitter (tinyland.dev PR #623). Apply only after tinyland.dev drops the bazel_dep (patches 02/03) and confirms zero remaining consumers via `bazel mod deps`."
  }
}
