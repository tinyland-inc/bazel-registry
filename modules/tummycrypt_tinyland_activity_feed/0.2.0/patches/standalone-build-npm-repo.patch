diff --git a/BUILD.bazel b/BUILD.bazel
index 8f4b51a..0abda1f 100644
--- a/BUILD.bazel
+++ b/BUILD.bazel
@@ -7,9 +7,9 @@ Dual-mode build: works with both pnpm (standalone) and Bazel (CI/production).
 """
 
 load("@aspect_rules_js//npm:defs.bzl", "npm_package")
+load("@tummycrypt_tinyland_activity_feed_npm//:defs.bzl", "npm_link_all_packages")
+load("@tummycrypt_tinyland_activity_feed_npm//:vitest/package_json.bzl", vitest_bin = "bin")
 load("@aspect_rules_ts//ts:defs.bzl", "ts_project")
-load("@npm//:defs.bzl", "npm_link_all_packages")
-load("@npm//:vitest/package_json.bzl", vitest_bin = "bin")
 
 npm_link_all_packages(name = "node_modules")
 
@@ -22,7 +22,10 @@ ts_project(
             "src/**/*.test.ts",
             "src/**/*.spec.ts",
         ],
-    ) + ["package.json"],
+    ) + [
+        "package.json",
+        "tsconfig.base.json",
+    ],
     declaration = True,
     declaration_map = True,
     source_map = True,
@@ -33,7 +36,6 @@ ts_project(
     validate = False,
     deps = [
         ":node_modules",
-        "//packages:tsconfig_base",
         ":node_modules/@types/node",
     ],
     visibility = ["//visibility:public"],
@@ -46,7 +48,7 @@ npm_package(
         ":tinyland_activity_feed",
     ],
     package = "@tummycrypt/tinyland-activity-feed",
-    version = "0.1.0",
+    version = "0.2.0",
     visibility = ["//visibility:public"],
 )
 
@@ -55,7 +57,7 @@ vitest_bin.vitest_test(
     data = [
         ":node_modules",
         "tsconfig.json",
-        "//packages:tsconfig_base",
+        "tsconfig.base.json",
         ":node_modules/vitest",
         "vitest.config.ts",
     ] + glob(["src/**/*.ts"]) + glob(["tests/**/*.test.ts", "tests/**/*.ts"]),
@@ -73,6 +75,7 @@ exports_files(
     [
         "package.json",
+        "tsconfig.base.json",
         "tsconfig.json",
         "vitest.config.ts",
     ],
     visibility = ["//visibility:public"],
diff --git a/pnpm-workspace.yaml b/pnpm-workspace.yaml
new file mode 100644
index 0000000..c6dab1d
--- /dev/null
+++ b/pnpm-workspace.yaml
@@ -0,0 +1,3 @@
+packages:
+  - "."
+onlyBuiltDependencies: []
