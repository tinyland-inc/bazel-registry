name: Immutability gate

on:
  pull_request:
  workflow_dispatch:

jobs:
  immutability-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Reject edits to shipped module versions
        run: sh scripts/check-immutable-versions.sh
